{
  "[html]": {
    // Because Jekyll adds liquid templating to HTML files, auto formatting
    // would remove line breaks between liquid tags, which are required for
    // readability.
    "editor.formatOnSave": false
  }
}
